import DashboardMenu from './DashboardMenu';

import * as S from './styled';

const Sidebar = () => {
  return (
    <S.SidebarContainer>
      <DashboardMenu />
    </S.SidebarContainer>
  );
};

export default Sidebar;
